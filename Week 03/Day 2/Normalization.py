# we clear the concept of normalization 
#import numpy as np 

#data = np.array([10, 20, 30, 30, 40, 50])

#normalized = (data - np.min(data)) / (np.max(data) - np.min(data))

#print(normalized)

#------------
# Example

#import numpy as np

#salary = np.array([50000, 10000,30000, 50500])

#normalized = (salary - np.min(salary)) / (np.max(salary) - np.min(salary))

#print(normalized)
